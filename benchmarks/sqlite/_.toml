name = "sqlite"

tool_name = "server"

type = "iter"

install = "cd files/ && unzip northwind-1659689066.zip && cd .. && cd scripts/tools/server && shards build"

run = "bash -c 'nohup scripts/tools/server/bin/server > /dev/null 2>&1 &' && sleep 5s && $COMMAND"

version = "scripts/tools/server/bin/server --help"
