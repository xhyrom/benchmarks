{
  "name": "benchmarks",
  "type": "module",
  "scripts": {
    "setup": "bun scripts/init.ts && bun scripts/setup_benchmarks.ts && bun scripts/setup_languages.ts",
    "bench": "./scripts/run_benchmarks.sh",
    "generate": "bun scripts/generate.ts",
    "all": "bun run setup && bun run bench && bun run generate"
  },
  "devDependencies": {
    "bun-types": "^0.1.8"
  },
  "dependencies": {
    "bun-utilities": "^0.2.1",
    "markdown-table": "^3.0.2",
    "mitata": "^0.1.6"
  }
}