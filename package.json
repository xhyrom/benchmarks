{
  "version": "1.0.0",
  "name": "benchmarks",
  "scripts": {
    "bench:console": "cd benchmarks/console && bun run bench",
    "bench:3rd-packages": "cd benchmarks/3rd-packages && bun run bench",
    "bench:json": "cd benchmarks/json && bun run bench",
    "bench:buffer": "cd benchmarks/buffer && bun run bench",
    "bench:http": "cd benchmarks/http && bun run bench",
    "bench": "bun run bench:console && bun run bench:3rd-packages && bun run bench:json && bun run bench:buffer && bun run bench:http",
    "generate:readme": "bun scripts/readme.ts"
  },
  "devDependencies": {
    "@types/node": "^18.6.1",
    "bun-types": "^0.1.4"
  },
  "dependencies": {
    "bun-utilities": "^0.1.8",
    "markdown-table": "^3.0.2",
    "mitata": "^0.1.6"
  }
}