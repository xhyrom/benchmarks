{
  "version": "1.0.0",
  "name": "benchmarks",
  "scripts": {
    "bench:console": "cd console && bun run bench",
    "bench:3rd-packages": "cd 3rd-packages && bun run bench",
    "bench:json": "cd json && bun run bench",
    "bench": "bun run bench:console && bun run bench:3rd-packages && bun run bench:json",
    "generate:readme": "bun scripts/readme.ts"
  },
  "devDependencies": {
    "bun-types": "^0.0.83"
  },
  "dependencies": {
    "bun-utilities": "^0.1.7",
    "markdown-table": "^3.0.2",
    "mitata": "^0.1.6"
  }
}