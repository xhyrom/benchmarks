language = "Go"
ext = "go"

enviroment = [
  { name = "main.go", install = "sudo apt update && sudo apt -y upgrade && sudo apt install golang-go", build = "go build $FILE", run = "./$FILE", version = "go version" }
]
