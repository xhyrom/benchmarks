language = "Ruby"
ext = "rb"

environment = [
  { name = "main.rb", install = "sudo apt update && sudo apt -y upgrade && sudo apt install -y ruby-full", run = "ruby $FILE$EXTENSION", version = "ruby --version" }
]
